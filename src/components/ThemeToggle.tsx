import React from 'react';

interface ThemeToggleProps {
  variant?: 'pill' | 'icon' | 'compact' | 'selector';
  className?: string;
  showPaletteMenu?: boolean;
}

/**
 * ThemeToggle component - Dihapus sesuai permintaan pengguna:
 * "hapus tema tampilan, hapus terang dan gelap dan samakan warna page dengan warna navigasi"
 */
export const ThemeToggle: React.FC<ThemeToggleProps> = () => {
  return null;
};
